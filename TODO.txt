//Download and install git
//Set this folder to git repo (maybe make git account?)
//download bower
//use bower to install angular, bootstrap, jquery, etc
//setup build flow
	//Compile js into one file
	//minify
	//compile less (if used)
	//publish to ftp (gulp has ftp module)